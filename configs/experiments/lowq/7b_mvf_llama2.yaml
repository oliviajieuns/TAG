# Low-quality-pool flagship: LLaMA-2-7B full FT, MVF, composite-20
# corrupted pool. Same env-var pool wiring as light_mvf_05b.yaml.
defaults:
  - configs/base.yaml
  - configs/methods/mvf.yaml
  - configs/models/llama2-7b.yaml
  - configs/modes/full_ft.yaml

output_subdir: lowq/mvf_llama2_7b
selection_ratio: 0.1
train_epochs: 3
use_8bit_optimizer: true
