# DeepSeek-7B + Alpaca-GPT4 random 10% — paper-faithful selection ratio
defaults:
  - configs/base.yaml
  - configs/methods/random.yaml
  - configs/models/deepseek-7b.yaml
  - configs/modes/full_ft.yaml

output_subdir: main_7b/deepseek/random_10
selection_ratio: 0.1
# epochs-unify(2026-05-16): 3 → 4. llama2 매트릭스(LLAMA-TUNE 1차)와 동일하게 통일.
train_epochs: 4
